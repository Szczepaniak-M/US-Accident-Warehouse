package pl.michalsz.spark
package model

case class FileLocation(zipcode: String,
                        city: String,
                        county: String,
                        state: String,
                        country: String,
                        Timezone: String)
